@model IEnumerable<_14548_employes_managment.Models.Instrument>

@{
    ViewData["Title"] = "Instruments";
}

<!-- Cabecalho da pagina da lista -->
<div class="content-header">
    <div class="container-fluid px-3 px-sm-0">
        <div class="row mb-2">
            <div class="col-12 col-sm-6">
                <h1 class="m-0">Instruments</h1>
            </div>
            <div class="col-12 col-sm-6">
                <ol class="breadcrumb float-sm-right float-left float-sm-right">
                    <li class="breadcrumb-item"><a href="/">Home</a></li>
                    <li class="breadcrumb-item active">Instruments</li>
                </ol>
            </div>
        </div>
    </div>
</div>

<!-- Tabela com os empregados -->
<div class="content">
    <div class="container-fluid px-3 px-sm-0">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header d-flex justify-content-between align-items-center flex-wrap">
                        <h3 class="card-title mb-2 mb-sm-0">List of Instruments</h3>
                        <div class="card-tools">
                            <a asp-action="Create" class="btn btn-primary btn-sm">
                                <i class="fas fa-plus"></i> <span class="d-none d-sm-inline">Add New Instrument</span>
                            </a>
                        </div>
                    </div>
                    <div class="card-body table-responsive">
                        <table id="example1" class="table table-bordered table-striped table-sm">
                            <thead>
                                <tr>
                                    <th style="min-width: 80px;">Id</th>
                                    <th style="min-width: 90px;">Instrument Type</th>
                                    <th style="min-width: 90px;">Instrument Name</th>
                                    <th style="min-width: 110px;">Use Strings</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@Html.DisplayFor(modelItem => item.Id)</td>
                                        <td>@Html.DisplayFor(modelItem => item.InstrumentType)</td>
                                        <td>@Html.DisplayFor(modelItem => item.InstrumentName)</td>
                                        <td>@Html.DisplayFor(modelItem => item.UseStrings)</td>
                                        <td>
                                            <div class="btn-group btn-group-sm" role="group">
                                                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info" title="View">
                                                    <i class="fas fa-eye"></i>
                                                </a>
                                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning" title="Edit">
                                                    <i class="fas fa-edit"></i>
                                                </a>
                                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger" title="Delete">
                                                    <i class="fas fa-trash"></i>
                                                </a>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
